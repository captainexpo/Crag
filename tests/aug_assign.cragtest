//NAME: Augmented Assignment
//DESC: self explanitory
//BEGIN CODE

import "stdlib" as std;

fn main(argc: i32, argv: **u8) -> i32 {
  let i: i32 = 0;
  i += 3;
  i -= 2;
  i *= 10;
  i /= 5;
  std.testing.assert(i == 2, "augmented assignment chain should result in 2");
  return 0;
}

//END CODE
