import "../stdlib/std.crag" as std

const printf = &std::cstdio::printf;

fn main() -> i32 {
    let a = std::vector::new::<i64>();
    a.append(0);
    a.append(1);
    for (let i: usize = 1; i < 100; i += 1) {
        a.append(a.get(i - 1).ok + a.get(i).ok);
    }
    for (let i: usize = 0; i < a.len(); i += 1){
        printf("%lld\n", a.get(i).ok);
    }
    return 0;
}
